services:
  - type: web
    name: elitebk-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn core.wsgi:application --bind 0.0.0.0:$PORT --timeout 120"
    postdeploy: "python manage.py createsuperuser --noinput || true"
